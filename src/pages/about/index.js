import React from "react";

export default function About() {
	return (
		<div>
			<p>
				Auto Email is an email automation software that sends email templates to
				contacts while customizing those templates with different values.
			</p>
		</div>
	);
}
